# Lean / Lake build artefacts
/.lake/

# Large raw data files (not needed for proof verification)
*.json
*.json.txt
*.ps

# Python cache
__pycache__/
*.pyc

# Log files (except build_status which is checked in as proof record)
cmd_status.log
